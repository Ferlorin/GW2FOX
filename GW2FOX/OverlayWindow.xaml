<Window x:Class="GW2FOX.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GW2FOX"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        Width="250"
        Height="370"
        ShowInTaskbar="False"
        Title="OverlayWindow"
        Name="MainOverlayWindow">

    <Window.Resources>
        <local:StrikeConverter x:Key="StrikeConverter"/>
        <local:PastEventColorConverter x:Key="PastEventColorConverter"/>

        <ControlTemplate x:Key="EmptyButtonTemplate" TargetType="RepeatButton">
            <Grid/>
        </ControlTemplate>
    </Window.Resources>

    <Grid x:Name="MainGrid" Background="Transparent">
        <Border Background="Transparent" BorderBrush="#333" BorderThickness="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Kopiert!-Nachricht -->
                <TextBlock x:Name="CopiedMessage"
                           Foreground="White"
                           Background="#AA000000"
                           FontSize="12"
                           Padding="6,2"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Top"
                           Margin="0,34,0,0"
                           Visibility="Collapsed"
                           Opacity="0"
                           Text="Kopiert!">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>

                <!-- Kopfzeile -->
                <Grid Grid.Row="0" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Image x:Name="Com"
                           Source="/Com.png"
                           Width="20"
                           Height="20"
                           Margin="6,5,4,5"
                           MouseLeftButtonDown="Icon_MouseLeftButtonDown"/>

                    <TextBlock Grid.Column="1"
                               VerticalAlignment="Center"
                               Foreground="White"
                               FontFamily="Segoe UI"
                               FontWeight="Bold"
                               FontSize="14"
                               Text="Boss-Timer">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="0" ShadowDepth="0" Opacity="1"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <Rectangle Grid.ColumnSpan="2"
                               VerticalAlignment="Bottom"
                               Height="1"
                               IsHitTestVisible="False">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#00FFFFFF" Offset="0.0"/>
                                <GradientStop Color="#66FFFFFF" Offset="0.5"/>
                                <GradientStop Color="#00FFFFFF" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </Grid>

                <!-- Inhalt -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                    </Grid.ColumnDefinitions>

                    <ScrollViewer x:Name="BossScrollViewer"
                                  Grid.Column="0"
                                  VerticalScrollBarVisibility="Hidden"
                                  HorizontalScrollBarVisibility="Disabled"
                                  Background="Transparent"
                                  ScrollChanged="BossScrollViewer_ScrollChanged">

                        <ListView x:Name="BossListView"
                                  BorderThickness="0"
                                  Background="Transparent"
                                  FontFamily="Segoe UI"
                                  FontWeight="Bold"
                                  FontSize="14"
                                  Padding="0"
                                  ScrollViewer.VerticalScrollBarVisibility="Disabled">

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal"
                                                VerticalAlignment="Center"
                                                Background="Transparent">

                                        <Image Source="{x:Static local:BossListItem.WaypointImage}"
                                               Width="20"
                                               Height="20"
                                               Margin="0,0,6,0"
                                               Cursor="Hand"
                                               MouseLeftButtonDown="Waypoint_MouseLeftButtonDown"
                                               VerticalAlignment="Center"/>

                                        <TextBlock Text="{Binding BossName}"
                                                   Foreground="{Binding IsPastEvent, Converter={StaticResource PastEventColorConverter}}"
                                                   TextDecorations="{Binding IsPastEvent, Converter={StaticResource StrikeConverter}}"
                                                   VerticalAlignment="Center"
                                                   Width="141"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Effect>
                                                <DropShadowEffect Color="Black" BlurRadius="0" ShadowDepth="0" Opacity="1"/>
                                            </TextBlock.Effect>
                                        </TextBlock>

                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>

                        </ListView>
                    </ScrollViewer>
                </Grid>

            </Grid>
        </Border>
    </Grid>
</Window>

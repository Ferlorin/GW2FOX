<Window x:Class="GW2FOX.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        Width="280" Height="400"
        ShowInTaskbar="False"
        Title="OverlayWindow">

    <Border CornerRadius="8" Background="Transparent" BorderBrush="#333" BorderThickness="0">
        <Grid>
            <!-- Drei Zeilen: Titelleiste, Linie, Inhalt -->
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <!-- Titelleiste -->
                <RowDefinition Height="1"/>
                <!-- Schwarze Linie -->
                <RowDefinition Height="*"/>
                <!-- Inhalt -->
            </Grid.RowDefinitions>

            <!-- Titelleiste -->
            <Grid Background="Transparent" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icon zum Verschieben -->
                <Image x:Name="Com" Source="/Com.png"
                       Width="20" Height="20" Margin="6,5,4,5"
                       MouseLeftButtonDown="Icon_MouseLeftButtonDown"/>

                <!-- Titel -->
                <TextBlock Grid.Column="1"
                           VerticalAlignment="Center"
                           Foreground="Black"
                           FontWeight="Bold"
                           FontSize="14"
                           Text="Event Timer">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black"
                                          BlurRadius="0"
                                          Direction="0"
                                          ShadowDepth="0"
                                          Opacity="1"/>
                    </TextBlock.Effect>
                </TextBlock>
            </Grid>

            <!-- 1-Pixel-Schwarze Linie unter der Titelleiste -->
            <Rectangle Grid.Row="1" Height="1" Fill="Black"/>

            <!-- Inhalt -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="93*"/>
                    <ColumnDefinition Width="185*"/>
                </Grid.ColumnDefinitions>
                <wfi:WindowsFormsHost Name="listViewHost" Grid.ColumnSpan="2"/>
            </Grid>
        </Grid>
    </Border>
</Window>
